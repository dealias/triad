MAGIC=magic # Hide default rule from Makefile.
include Makefile

$(NAME): $(FILES:=.o) $(CFILES:=.o)
	$(CXX) $(OPT) -o $(NAME) $(FILES:=.o) $(CFILES:=.o) $(LIB)

include $(FILES:=.$(D))

ifdef CFILES
include $(CFILES:=.$(D))
endif
