.cc.o:
	$(C++) $(OPT) $(INCL) -o $@ -c $<

clean:
	rm -f *.o *mon.out $(ALL)

%.$(D): %.cc
	$(MAKEDEPEND) $(INCL) $(MDOPTS) $< > $@

all: $(TRIAD:=.o)
	$(C++) $(OPT) -o triad $(TRIAD:=.o) $(LIB)

rgb: $(RGB:=.cc)
	$(C++) $(OPT) -o $(HOME)/bin/rgb $(RGB:=.cc) $(LIB)

include $(TRIAD:=.$(D))

