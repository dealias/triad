DEFAULT = default
NAME = triad
D = d
LIB = $(LFLAGS) -lm
OPT = -g $(CFLAGS)
INCL = -I. -I$(TRI) -I-

vpath %.cc $(TRI)

include $(TRI)/config/$(HOSTTYPE)

ifdef Cpp
C++ = $(Cpp)
endif

UTILS = utils msg strcasecmp new $(ARCH) $(POLL)
CORE = kernel Integrator Param

.SUFFIXES: .cc
