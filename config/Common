D = d
LIB = $(LFLAGS) -lm
xOPT = -g $(CFLAGS)
INCL = -I . -I $(TRI) -I-

vpath %.cc $(TRI)

include $(TRI)/config/$(HOSTTYPE)

UTILS = utils strcasecmp new $(ARCH) $(POLL)
CORE = kernel Integrator Param

.SUFFIXES: .cc
